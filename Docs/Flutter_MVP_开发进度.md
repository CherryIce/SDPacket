# Flutter MVP 开发进度

更新日期：2026-08-26  
当前版本：`0.1.0+1`

## 已实现

- Flutter 双平台工程，最低支持 iOS 15、Android 14（API 34）。
- 本地 JSON 数据快照与应用支持目录存储；写入先生成临时文件，并保留上一份备份。
- 新用户可删除示例项目；示例删除后不会在下次启动时重新生成。
- 项目创建、编辑、归档、恢复、删除和进度统计。
- 自动箱号、项目内重复校验、删除后不复用编号。
- 手动空白建箱、相机单张登记、系统照片选择器最多 30 张批量登记。
- 每张批量照片复制到应用目录并逐项创建草稿，单项失败不回滚已经成功的记录。
- 语音转写形成可编辑备注；设备、语言或权限不支持时可立即回到文字登记。
- 箱子标题、目的房间、当前位置、备注、标签、结构化物品、优先级、搬运状态和异常标记编辑。
- 待实体标记提醒，以及二维码、手写、便利贴和其他方式的独立确认状态。
- 项目内及全局离线包含匹配搜索。
- QR 载荷 `MOVEBOX|v=1|project=...|box=...|code=...`，以稳定 UUID 定位记录。
- QR 扫描、高清 PNG、单标签 PDF、A4 批量 PDF、系统分享和打印入口。
- “标签已导出”和“实体标记已确认”分别记录；取消分享/打印不会标记为已导出。
- CSV 导出；schema v1 JSON 备份、引用/重复箱号校验及失败不覆盖恢复。
- 简体中文/英文 Flutter 文案，iOS 权限用途和双平台应用名称本地化。

## 当前验证证据

- `flutter analyze`：通过，无问题。
- `flutter test`：9 项通过，覆盖编号不复用、重复箱号、搜索、状态统计、QR 解析、无效备份不覆盖、中文 UTF-8 备份和首页主入口。
- `flutter build ios --simulator --no-codesign`：通过。
- iPhone 17 Pro / iOS 26.5 模拟器：安装、启动及中文首页显示通过。
- `flutter build apk --debug`：通过，生成 Debug APK。
- `plutil -lint`、Android XML、Xcode 工程文件和最低系统版本检查：通过；产物确认 iOS `MinimumOSVersion=15.0`、Android `sdkVersion=34`。

以上证据不代表真机、iOS 15 运行时、Android 14 运行时、打印机、离线语音、App Store、Google Play 或 Release 签名已经验收。

## 下一批 P0

1. 增加连续相机批次、批次会话模型、“保存并下一箱”和进程被系统终止后的批次恢复。
2. 处理 Android `image_picker.retrieveLostData()`，避免系统回收 Activity 后遗漏选图结果。
3. 将备份从 JSON + 照片路径升级为带清单、校验和与照片文件的版本化备份包；恢复时处理缺图和 ID/箱号冲突策略。
4. 完善结构化物品的数量与备注编辑，并增加箱子状态历史。
5. 用真实 A4 黑白打印、折痕/反光标签和损坏二维码完成打印扫描矩阵。
6. 在 iOS 15 真机和 Android 14 真机验证相机、照片、麦克风、语音、拒绝权限、扫码、分享、打印和飞行模式。
7. 确认正式名称、图标、生产 Bundle ID/Application ID 及双平台 Release 签名后再进入发布准备。
